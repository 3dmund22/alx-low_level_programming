#include <stdio.h>

/*
 *Prints single digit numbers of base 10 from 0
 */
 
int main(void)
{
  int num = 0;

  while(num < 0)
    {
      printf("%i", num);
      n++;
    }
  putchar('\n');

  return (0);

}
