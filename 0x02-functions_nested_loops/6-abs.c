#include "holberton.h"

/**
 * abs - computes absolute value of an integer.
 *
 * Return: 0.
 */

int _abs(int)
{
  if (n > 0)
    {
      return (n);
    }
  else if (n == 0)
    {
      return (0);
    }
  else
    {
      return (-n);
    }
  return 0;
}
